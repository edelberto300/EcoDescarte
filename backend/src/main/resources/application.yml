spring:
  application:
    name: ecodescarte
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/ecodescarte?serverSelectionTimeoutMS=5000}
      auto-index-creation: true

server:
  port: 8080
